# CVE-2026-69247 (cryptography, HIGH): fixed in 50.0.0, but mlflow==3.15.2
# pins cryptography<50, and cryptography 50.0.1 was confirmed to break
# `pip check` when tried. Currently pinned to 49.0.0 (past every earlier
# fix boundary Trivy reported) as the highest version mlflow allows.
# Revisit once mlflow relaxes its cryptography constraint.
# See docs/ENGINEERING_LOG.md, Entry 12.
CVE-2026-69247
